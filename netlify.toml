[build]
  command   = "npm run build:client"
  publish   = "dist"

[build.environment]
  NODE_VERSION = "20"

# SPA fallback — all routes served via index.html
[[redirects]]
  from   = "/*"
  to     = "/index.html"
  status = 200
